// pyramid builder

function count(start, end) {
  for (; start < end; start++) {
    document.write(start);
  }
}

var oldNumber = 1;
var newNumber = 6;
count(oldNumber, newNumber);
count(start, end);

